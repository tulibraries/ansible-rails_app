---

## dependencies.yml
# default to no app specific dependencies to install
rails_app_additional_dependencies: []
nodejs_version: 6.17.1
rails_app_use_webpack: true
yarn_version: v1.13.0

rails_app_user: "{{ rails_app_name }}"
rails_app_git_branch: master
rails_app_install_path: /var/www/{{ rails_app_name }}
rails_app_ruby_path: /home/{{ rails_app_user }}/.rbenv/versions/{{ rbenv.default_ruby}}
rails_app_bundle_exe: "{{rails_app_ruby_path}}/bin/bundle"
rails_app_gem_path: "{{rails_app_ruby_path}}/lib/ruby/gems"
rails_app_url_base_path: "/"
rails_app_use_ssl: false
rails_app_force_ssl: false
rails_app_default_config:
  - secrets.yml
# Override to add custom config files from templates
rails_app_additional_config: []
rails_app_env: production
rails_app_deployment_exclude_groups:
  - test
  - development

is_blacklight_app: false
app_use_ssl: false
app_certs_base_path: /etc/pki/tls
app_ssl_cert_path: "{{ app_certs_base_path }}/certs/{{ rails_app_name }}.crt"
app_ssl_key_path: "{{ app_certs_base_path }}/private/{{ rails_app_name }}.key"
app_ssl_intermediate_path: "{{ app_certs_base_path }}/certs/intermediate.crt"
app_use_intermediate_cert: true
app_set_root_redirect: false

using_postgres: false

# AWS Logs Defaults
awslogs_region: us-east-1
awslogs_access_key_id: SOMETHING_ENCRYPTED
awslogs_secret_access_key: SOMETHING_ENCRYPTED
aws_group_name: "{{ rails_app_name }}_test"
awslogs_logs:
  - file: "/var/www/{{ rails_app_name }}/log/development.log"
    format: "%b %d %H:%M:%S"
    time_zone: "LOCAL"
    initial_position: "end_of_file"
    group_name: "{{ aws_group_name }}"
    stream_name: "development.log"
  - file: "/var/www/{{ rails_app_name }}/log/production.log"
    format: "%b %d %H:%M:%S"
    time_zone: "LOCAL"
    initial_position: "end_of_file"
    group_name: "{{ aws_group_name }}"
    stream_name: "production.log"
  - file: "/var/www/{{ rails_app_name }}/log/skylight.log"
    format: "%b %d %H:%M:%S"
    time_zone: "LOCAL"
    initial_position: "end_of_file"
    group_name: "{{ aws_group_name }}"
    stream_name: "skylight.log"
  - file: "/var/log/shibboleth/shibd.log"
    format: "%b %d %H:%M:%S"
    time_zone: "LOCAL"
    initial_position: "end_of_file"
    group_name: "{{ aws_group_name }}"
    stream_name: "shibd.log"
  - file: "/var/log/shibboleth/shibd_warn.log"
    format: "%b %d %H:%M:%S"
    time_zone: "LOCAL"
    initial_position: "end_of_file"
    group_name: "{{ aws_group_name }}"
    stream_name: "shibd_warn.log"
  - file: "/var/log/shibboleth/signature.log"
    format: "%b %d %H:%M:%S"
    time_zone: "LOCAL"
    initial_position: "end_of_file"
    group_name: "{{ aws_group_name }}"
    stream_name: "signature.log"
  - file: "/var/log/shibboleth/transaction.log"
    format: "%b %d %H:%M:%S"
    time_zone: "LOCAL"
    initial_position: "end_of_file"
    group_name: "{{ aws_group_name }}"
    stream_name: "transaction.log"
  - file: "/var/log/shibboleth-www/native.log"
    format: "%b %d %H:%M:%S"
    time_zone: "LOCAL"
    initial_position: "end_of_file"
    group_name: "{{ aws_group_name }}"
    stream_name: "shibboleth-www/native.log"
  - file: "/var/log/shibboleth-www/native_warn.log"
    format: "%b %d %H:%M:%S"
    time_zone: "LOCAL"
    initial_position: "end_of_file"
    group_name: "{{ aws_group_name }}"
    stream_name: "shibboleth-www/native_warn.log"
  - file: "/var/log/httpd/access_log"
    format: "%b %d %H:%M:%S"
    time_zone: "LOCAL"
    initial_position: "end_of_file"
    group_name: "{{ aws_group_name }}"
    stream_name: "httpd/access_log"
  - file: "/var/log/httpd/error_log"
    format: "%b %d %H:%M:%S"
    time_zone: "LOCAL"
    initial_position: "end_of_file"
    group_name: "{{ aws_group_name }}"
    stream_name: "httpd/error_log"
  - file: "/var/log/httpd/old_url_ssl_access_log"
    format: "%b %d %H:%M:%S"
    time_zone: "LOCAL"
    initial_position: "end_of_file"
    group_name: "{{ aws_group_name }}"
    stream_name: "httpd/old_url_ssl_access_log"
  - file: "/var/log/httpd/ssl_access_log"
    format: "%b %d %H:%M:%S"
    time_zone: "LOCAL"
    initial_position: "end_of_file"
    group_name: "{{ aws_group_name }}"
    stream_name: "httpd/ssl_access_log"
  - file: "/var/log/httpd/ssl_error_log"
    format: "%b %d %H:%M:%S"
    time_zone: "LOCAL"
    initial_position: "end_of_file"
    group_name: "{{ aws_group_name }}"
    stream_name: "httpd/ssl_error_log"
  - file: "/var/log/httpd/ssl_request_log"
    format: "%b %d %H:%M:%S"
    time_zone: "LOCAL"
    initial_position: "end_of_file"
    group_name: "{{ aws_group_name }}"
    stream_name: "httpd/ssl_request_log"
